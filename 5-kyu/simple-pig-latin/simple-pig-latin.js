 
function pigIt(str){
  //Code here
  let swich = str.split('')
  let chnge = swich.map(
  if(!isNaN(swich.charAt(0)) ){
    return swich
  }
    return swich.slice(1) + word[0] + 'ay'.join('')
  )
​
}