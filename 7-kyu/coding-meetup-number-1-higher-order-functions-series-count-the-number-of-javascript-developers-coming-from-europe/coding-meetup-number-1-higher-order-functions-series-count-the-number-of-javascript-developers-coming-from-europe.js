function countDevelopers(list) {
  // your awesome code here :
let ans = list.filter((developer)=> developer.continent=== 'Europe' && developer.language=== 'JavaScript').length;  return ans
  return ans
}